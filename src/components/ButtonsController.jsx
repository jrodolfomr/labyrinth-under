export const ButtonsController = () => {
  return (
    <div>
      {/* <div className='control-panel'>
        <button onClick={() => moveSprite('left')}>Left</button>
        <button onClick={() => moveSprite('right')}>Right</button>
        <button onClick={() => moveSprite('up')}>Up</button>
        <button onClick={() => moveSprite('down')}>Down</button>
        <button onClick={() => toggleCreation()}>
          {isCreativeCell ? 'cellChange' : 'wallchange'}
        </button>
      </div> */}
      {/* <div className='levelchanger'>
        <button onClick={() => changeLevel(1)}>Level 1</button>
        <button onClick={() => changeLevel(2)}>Level 2</button>
        <button onClick={() => changeLevel(3)}>Level 3</button>
        <button onClick={() => changeLevel(4)}>Level 4</button>
        <button onClick={() => changeLevel(5)}>Level 5</button>
      </div> */}
      {/* <div className='otherbuttons'>
        <button onClick={() => saveLevelData()}>Save LevelDatea</button>

        <button onClick={() => handleBacktrack()}>BackTrack Move</button>

        <button onClick={() => handleHammer('up')}>Use Hammer Up</button>
        <button onClick={() => handleHammer('down')}>Use Hammer Down</button>
        <button onClick={() => handleHammer('left')}>Use Hammer Left</button>
        <button onClick={() => handleHammer('right')}>Use Hammer Right</button>
      </div> */}
      {/* <h1>{stack.length}</h1>
      {stack[stack.length - 1]} */}
    </div>
  )
}
